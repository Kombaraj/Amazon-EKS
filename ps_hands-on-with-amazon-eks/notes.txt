git clone https://github.com/kombaraj/amazon-eks.git

chmod -R 700 amazon-eks

cd amazon-eks/ps_hands-on-with-amazon-eks

# Install nano, eksctl, helm
./script-by-chapter/prepare-cloud-shell.sh

# Install EKS Cluster
./script-by-chapter/chapter-1.sh

# Install AWS Loadbalancer Controller & create IAM Policy for ELB access
cd ./Infrastructure/k8s-tooling/load-balancer-controller
./create.sh

To make EKS Worker nodes to communicate with Loadbalancer, map the IAM Policy ARN which just created to the NodeInstanceRole created from eks-acg-nodegroup Cloudformation templete

cd test
./run.sh

Validate ALB created and accessible


# Create ACM SSL certificate
cd ./Infrastructure/clouformation/ssl-certificate
./create.sh

